
        /**Java provides a mechanism tha allows you define a variable, initialize it and moreover fix the variable's value
         * so that it cannot be changed.
         * SYNTAX
         * public static final Type Variable = Constant value;
         *
         * example:
         */
        public static final double PI = 3.14159;

        //the convention for naming constants is to use all UPPERCASE letters with an underscore symbol between words
        public static final int DAYS_PER_WEEK = 7;
        public static final String ADAGIO = "the customer is right!";